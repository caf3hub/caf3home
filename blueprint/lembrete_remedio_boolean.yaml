blueprint:
  name: Lembrete Remédio - Conversation AI
  description: Cria lembretes de remédio em determinados horários e permite integração via MQTT.
  domain: automation
  input:
    notification_target:
      name: Serviço de Notificação
      description: Serviço de notificação que irá enviar a mensagem.
      default: "notify.mqtt_notify"
      selector:
        text:
    conversation:
      name: Serviço de Conversation AI
      description: Serviço de conversation ai que irá gerar a mensagem.
      default: "conversation.google_generative_ai"
      selector:
        text:
    remedio_lista:
      name: Lista de Remédios
      description: Insira os remédios no formato - Nome, HH:MM, Descrição
      selector:
        text:
          multiline: true
    lembrete_boolean:
      name: Input Boolean Lembrete Remédio
      description: O boolean que controla o status de lembrete de remédio.
      selector:
        entity:
          domain: input_boolean

variables:
  remedio_lista: !input remedio_lista
  lembrete_boolean: !input lembrete_boolean

triggers:
  - platform: mqtt
    topic: home/telegram/message
    id: remedio_mqtt
  - platform: mqtt
    topic: home/telegram/message/user
    payload: remedio_tomado
    id: remedio_mqtt_user
  - platform: state
    entity_id: !input lembrete_boolean
    to: "on"
    id: lembrar_remedio
  - platform: time_pattern
    minutes: /1
    id: hora_remedio

conditions: []

actions:
  - alias: preparar texto
    if:
      - condition: trigger
        id: hora_remedio
      - condition: template
        value_template: >
          {% set horario_atual = now().strftime('%H:%M') %}
          {% set ns = namespace(horarios=[]) %}
          {% for remedio in remedio_lista.split('\n') %}
            {% set remedio_dados = remedio.split(', ') %}
            {% if remedio_dados | length > 1 %}
              {% set ns.horarios = ns.horarios + [remedio_dados[1].strip()] %}
            {% endif %}
          {% endfor %}
          {{ horario_atual in ns.horarios }}
    then:
      - service: conversation.process
        data:
          text: >-
            O nome do seu mestre e pai é Ruan, mas me chame de Mestre apenas.
            Você vai enviar mensagens para o Telegram ou para Alexa.
            Varie o estilo da mensagem a cada vez que for gerada.
            Não quero sugestões, mande apenas uma mensagem.
            Não envie "\n" e nem tente fazer quebras de linha assim, nem nada disso pois não funciona no Telegram.
            Se for quebrar linhas, apenas faça como se tivesse dando um enter e continue a digitar.
            Crie uma mensagem única e personalizada, de maneira criativa e variada, para informar que tá na hora de tomar o remédio.
            - Inclua as seguintes informações sobre os remédios:
              - Nome do remédio
              - Horário de tomar
              - Instruções ou descrição
            Remédios para Agora:
            {%- set remedios = remedio_lista.split('\n') -%}
            {%- set ns = namespace(encontrou=false) -%}
            {%- for remedio in remedios -%}
              {%- set remedio_dados = remedio.split(', ') -%}
              {%- if remedio_dados | length > 2 and remedio_dados[1].strip() == horario_atual -%}
                - **Remédio**: {{ remedio_dados[0].strip() }} 💊  
                  **Horário**: {{ remedio_dados[1].strip() }} ⏰  
                  **Instrução**: {{ remedio_dados[2].strip() }}  
                {%- set ns.encontrou = true -%}
              {%- endif -%}
            {%- endfor %}
            {%- if not ns.encontrou -%}
              Nenhum remédio a ser tomado agora.
            {%- endif %}
          agent_id: !input conversation
        response_variable: agent

  - alias: Enviar notificação
    service: !input notification_target
    data:
      title: "{{ now().strftime('%A') }} Lembrete de Remédios"
      message: "{{ agent.response.speech.plain.speech }}"

  - alias: Se for notificado sobre algum remédio
    if:
      - condition: trigger
        id: remedio_mqtt
      - condition: template
        value_template: "{{ 'Lembrete de Remédios' in trigger.payload_json.title }}"
    then:
      - service: input_boolean.turn_on
        target:
          entity_id: !input lembrete_boolean

  - alias: Se receber remedio_tomado no user
    if:
      - condition: trigger
        id: remedio_mqtt_user
    then:
      - service: input_boolean.turn_off
        target:
          entity_id: !input lembrete_boolean

  - alias: lembrar novamente do remédio
    if:
      - condition: trigger
        id: lembrar_remedio
    then:
      - repeat:
          while:
            - condition: state
              entity_id: !input lembrete_boolean
              state: "on"
          sequence:
            - delay:
                hours: 0
                minutes: 1
                seconds: 0
                milliseconds: 0
            - service: script.enviar_mensagem_personalizada_com_ia_generativa
              data:
                notification_target: !input notification_target
                prompt: >-
                  Você é a FRIDAY do Homem de Ferro, e uma assistente pessoal da minha casa.
                mensagem: >-
                  Você tomou seu remédio? Se já tomou pode desconsiderar, se não tomou é melhor tomar.

mode: parallel
max: 3
